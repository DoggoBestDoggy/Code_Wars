Tout d'abord on "return" une "list" ce qui nous permettra de placer des éléments
donc des chiffres entre crochet. Par la suite on mets la function "range" ce qui
nous permettra d'effectuer une action un certain nombre de fois. Ensuite on commence
a compter a partir de 1 dans ce cas la et on veut compter de 1 en 1 donc on fait
(1, n+1)